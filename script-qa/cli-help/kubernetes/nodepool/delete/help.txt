Deletes a node pool in a Kubernetes cluster by nodepool_uuid.

Usage:
  ./cli kubernetes nodepool delete [cluster-id] [id] [flags]

Flags:
      --cluster-id uuid   Cluster's UUID. (required)
  -h, --help              help for delete
      --id uuid           Nodepool's UUID. (required)
  -v, --version           version for delete

Global Flags:
      --cli.show-cli-globals   Show all CLI global flags on usage text
      --env enum               Environment to use (one of "pre-prod" or "prod") (default "prod")
      --region enum            Region to reach the service (one of "br-mgl-1", "br-ne-1" or "br-se-1") (default "br-ne-1")
      --server-url uri         Manually specify the server to use

